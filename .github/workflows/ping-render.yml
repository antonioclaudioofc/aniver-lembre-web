name: Ping Render App

on:
  schedule:
    - cron: "*/5 * * * *" # a cada 5 minutos
  workflow_dispatch: # permite rodar manualmente

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call Render App
        run: |
          curl -s https://aniver-lembre.onrender.com > /dev/null
